---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as: Tag = "div", class: className = "" } = Astro.props;
---

<Tag class={`mx-auto w-full max-w-6xl px-6 md:px-8 lg:px-10 ${className}`}>
  <slot />
</Tag>
